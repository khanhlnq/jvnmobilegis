How to run JVNMobileGIS emulator:
- Execute "run.bat" (in Windows) or "run.sh" (in Linux) to run JVNMobileGIS emulator.

NOTE: You must have JRE (Java Runtime Environment - http://java.sun.com) installed before running the emulator.
